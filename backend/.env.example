PORT=4000
NODE_ENV=development
DATABASE_PATH=./data/nnawa.sqlite
SESSION_DB_PATH=./data/sessions.sqlite

# Required in production. In development a fixed fallback is used with a warning.
SESSION_SECRET=

# Used only by the create-admin script, never at runtime.
ADMIN_EMAIL=nnawa_admin@gmail.com
ADMIN_PASSWORD=
